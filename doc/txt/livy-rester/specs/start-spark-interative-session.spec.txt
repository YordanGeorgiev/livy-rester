# doc/txt/livy-rester/spec/start-spark-interative-session.spec.txt

	doSpecStartSparkInterativeSession
	
	# Purpose:
	# ------------------------------------------------------
	-- todo: what is the purpose of the doSpecStartSparkInterativeSession function

	# Prequisites
	# ------------------------------------------------------
	-- todo: what are the prerequisites of the doSpecStartSparkInterativeSession function

	# Installation
	# ------------------------------------------------------
	-- todo: what are the required installations for the  
		doSpecStartSparkInterativeSession function to execute without errors

	# Interdependancies
	# ------------------------------------------------------
	-- todo: what are the interdependancies for the  
		doSpecStartSparkInterativeSession function to execute without errors

	# Usage:
	# ------------------------------------------------------
	-- todo: how can be the doSpecStartSparkInterativeSession used 

	# Developent
	# ------------------------------------------------------
	-- todo: what issues, todos could be pointed for development

	# Caveats:
	# ------------------------------------------------------
	-- todo: what are the known caveats of the doSpecStartSparkInterativeSession functions


# eof file: doc/txt/livy-rester/spec/start-spark-interative-session.spec.txt
