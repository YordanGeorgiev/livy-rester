# doc/txt/livy-rester/spec/stop-spark-interative-session.spec.txt

	doSpecStopSparkInterativeSession
	
	# Purpose:
	# ------------------------------------------------------
	-- todo: what is the purpose of the doSpecStopSparkInterativeSession function

	# Prequisites
	# ------------------------------------------------------
	-- todo: what are the prerequisites of the doSpecStopSparkInterativeSession function

	# Installation
	# ------------------------------------------------------
	-- todo: what are the required installations for the  
		doSpecStopSparkInterativeSession function to execute without errors

	# Interdependancies
	# ------------------------------------------------------
	-- todo: what are the interdependancies for the  
		doSpecStopSparkInterativeSession function to execute without errors

	# Usage:
	# ------------------------------------------------------
	-- todo: how can be the doSpecStopSparkInterativeSession used 

	# Developent
	# ------------------------------------------------------
	-- todo: what issues, todos could be pointed for development

	# Caveats:
	# ------------------------------------------------------
	-- todo: what are the known caveats of the doSpecStopSparkInterativeSession functions


# eof file: doc/txt/livy-rester/spec/stop-spark-interative-session.spec.txt
