# doc/txt/livy-rester/test/start-spark-interative-session.test.txt

	doTestStartSparkInterativeSession
	
	# Purpose:
	# ------------------------------------------------------
	-- todo: what is the purpose of the doTestStartSparkInterativeSession function

	# Prequisites
	# ------------------------------------------------------
	-- todo: what are the prerequisites of the doTestStartSparkInterativeSession function

	# Installation
	# ------------------------------------------------------
	-- todo: what are the required installations for the  
		doTestStartSparkInterativeSession function to execute without errors

	# Interdependancies
	# ------------------------------------------------------
	-- todo: what are the interdependancies for the  
		doTestStartSparkInterativeSession function to execute without errors

	# Usage:
	# ------------------------------------------------------
	-- todo: how can be the doTestStartSparkInterativeSession used 

	# Developent
	# ------------------------------------------------------
	-- todo: what issues, todos could be pointed for development

	# Caveats:
	# ------------------------------------------------------
	-- todo: what are the known caveats of the doTestStartSparkInterativeSession functions


# eof file: doc/txt/livy-rester/test/start-spark-interative-session.test.txt
